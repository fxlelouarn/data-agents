{
  "name": "@data-agents/database",
  "version": "1.0.0",
  "description": "Database layer for data agents",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prisma:generate": "prisma generate",
    "build": "tsc",
    "dev": "tsc --watch",
    "tsc": "tsc --noEmit",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "push": "prisma db push",
    "studio": "prisma studio",
    "seed": "tsx prisma/seed.ts",
    "seed:consensus": "ts-node seed-consensus-example.ts"
  },
  "dependencies": {
    "@data-agents/types": "*",
    "@prisma/client": "^6.17.1",
    "@types/bcrypt": "^5.0.2",
    "bcrypt": "^5.1.1",
    "date-fns-tz": "^3.2.0",
    "meilisearch": "^0.53.0",
    "prisma": "^6.17.1",
    "ts-node": "^10.9.2",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "prisma": "^6.17.1",
    "tsx": "^4.0.0",
    "typescript": "^5.0.4"
  }
}
